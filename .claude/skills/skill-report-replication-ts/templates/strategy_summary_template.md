# 策略逻辑摘要（strategy_summary.md）— 模板

> 复制本文件到 `02_strategy_logic/strategy_summary.md` 后填空。`<...>` 是占位符。
> **抽取默认值**（研报未明确时填默认并在"抽取溯源"标"默认值"）：
> regime=无 ｜ 开仓=双均线 ｜ 止盈止损=ATR吊灯 ｜ 仓位控制=满仓 ｜ 参数优化=网格
> `check_strategy_logic.py` 检查：研究问题/结论、资产池、周期、开仓、止损、止盈、参数、假设、**regime、仓位、参数优化**。

---

## 0. 报告信息

- 报告标题：`<原文标题>`
- 来源：`<URL 或本地 PDF 路径>`
- 研究员/机构：`<如：何康/徐特 华泰金工>`
- 发布日期：`<YYYY-MM-DD>`　复现日期：`<YYYY-MM-DD>`
- **报告类型**：`<策略型 / 方法论综述（取其实证策略部分）>`

## 1. 原文思路总结（指标设计目的与原理）

> 说明原文每个指标/模块**为什么这么设计**——背后的市场假设与统计学/经济学原理。这是"可解释性"的核心，非流水账复述。

- **核心立论**：`<如：价格 = 长期趋势μ + 短期惯性ρ + 噪声σ；趋势可交易性 = 趋势信号能否对抗噪声>`
- **各模块设计目的**：
  - regime 判断：`<如：TSI=μ/σ 量化可交易性，ρ 判趋势/震荡，避免震荡市磨损>`
  - 开仓：`<如：均线突破捕捉趋势启动；线性滤波本质是历史收益加权>`
  - 止盈止损：`<如：ATR吊灯"截断亏损让利润奔跑"，适应异方差波动>`
  - 仓位控制：`<如：vol-target 让不同波动期风险一致>`
- **关键原理引用**：`<如：Levine & Pedersen 2016"所有线性趋势指标都是历史收益加权"；Moskowitz 2011 时间序列动量>`
- 抽取自：`<原文"核心观点"/"导读"章节>`

## 2. 研究问题与结论

- **研究问题**：`<一句话>`
- **核心结论**：`<研报结论，含关键数值如"卡玛比从 X 提升到 Y">`
- 抽取自：`<原文章节>`

## 3. 资产池与样本

- **资产池**：`<品种/universe>`
- **样本区间**：`<如 2020-01 ~ 2026-05>`　**频率**：`<日频/周频，rebalance 收盘>`
- 抽取自：`<原文"实证分析">`

## 4. 数据源（外部本地路径，技能不下载）

- 行情路径：`<如 /data/market/hc_1d.csv>`　列：`date, symbol, open, high, low, close, volume`
- 复权 / 可用时点假设 / 缺失值处理：`<...>`
- 记入 `manifest.json` 的 data_sources。

## 5. 市场状态 / regime 判断

- **是否做 regime 判断**：`<是 / 否>`
- **方法**：`<如：TSI 置信区间 + ρ；或 Hurst；或 不做（默认：无）>`
- **状态划分**：`<如：趋势态 / 震荡态；或 不区分>`
- **门控作用**：`<如：仅趋势态开趋势仓，震荡态切反转或空仓；或 不门控>`
- **默认值**：无（研报未提 regime 时不做门控，全程按开仓信号交易）
- 抽取自：`<原文"市场状态识别"章节 / 默认值>`

## 6. 开仓规则（Entry）

- **信号类型**：`<双均线交叉 / 动量 / 突破 / RSI反转 / Kalman状态 / 其他>`
- **方向**：多（direction=1）/ 空（direction=-1）
- **触发条件**：`<如：快线上穿慢线做多>`
- **参数**：`<快窗/慢窗/阈值>`
- **默认值**：双均线交叉（快窗 5 / 慢窗 20），研报未明确开仓时用此
- 抽取自：`<原文"开仓信号"章节 / 默认值>`

## 7. 止盈止损规则（Take-profit / Stop-loss）

- **止损类型**：`<固定% / ATR静态 / ATR移动（吊灯）/ 逻辑证伪>`
- **止盈/退出类型**：`<移动止盈 / 时间强制退出 / 目标位 / 逻辑证伪>`
- **参数**：`<ATR周期=14，倍数 k=2.0；TR 是否 5%-95% 截尾；最大持有 N 根>`
- **触发动作**：`direction = 0（平仓）`
- **默认值**：ATR 吊灯（移动止损/止盈，k=2.0），研报未明确时用此
- 抽取自：`<原文"退出机制"章节 / 默认值>`

## 8. 风控

- **成本**：手续费 `<bps>`、滑点 `<bps>`
- **保证金 / 杠杆**：`<合约乘数、保证金率>`
- **执行假设**：`<执行滞后 1 根；止损在 strategy.py 用 OHLC 判断后以 direction 体现，日频口径>`
- **已知局限**：`<日频 bar；非 intraday 原生止损；gap 风险>`
- 抽取自：`<原文"风险提示" + backtest_engine.md 已知局限>`

## 9. 仓位控制（Position sizing）

- **方法**：`<满仓 / vol-target / 等手数 / 风险平价 / Kelly>`
- **参数**：`<如：目标年化波动 15%；单品种最大权重；保证金占用>`
- **默认值**：满仓（信号为 ±1 即满仓多/空），研报未明确时用此
- 抽取自：`<原文"仓位/资金管理"章节 / 默认值>`

## 10. 参数设置

| 参数 | 值 | 含义 | 来源 |
|------|-----|------|------|
| `<fast_window>` | `<5>` | `<快线窗口>` | `<原文/默认>` |
| `<slow_window>` | `<20>` | `<慢线窗口>` | `<原文/默认>` |
| `<atr_period>` | `<14>` | `<ATR周期>` | `<原文/默认>` |
| `<atr_mult>` | `<2.0>` | `<止损倍数 k>` | `<原文/默认>` |
| `<target_vol>` | `<0.15>` | `<目标年化波动（若 vol-target）>` | `<原文/默认>` |

## 11. 参数优化（Parameter optimization）

- **是否做优化**：`<是 / 否>`
- **方法**：`<网格 / 贝叶斯 / 遗传 / walk-forward 优化 / 不优化（默认：网格）>`
- **搜索空间**：`<如：fast∈{3,5,10}, slow∈{15,20,30}, k∈{1.5,2,2.5,3}>`
- **过拟合控制**：`<如：IS/OOS 切分、walk-forward、样本外验证>`
- **默认值**：网格搜索（在合理范围内网格扫参 + IS/OOS 防过拟合），研报未明确时用此
- 抽取自：`<原文"参数优化/稳健性"章节 / 默认值>`

## 12. 抽取溯源表

| 要素 | 取值 | 来源（原文章节/公式/默认值） | 复现实现位置 |
|------|------|------------------------------|--------------|
| regime | `<...>` | `<§X / 默认:无>` | `reference_implementation.py: regime_state()` |
| 开仓 | `<...>` | `<§X / 默认:双均线>` | `entry_signal()` |
| 止盈止损 | `<...>` | `<§X / 默认:ATR吊灯>` | `stop_loss()/take_profit()` |
| 仓位控制 | `<...>` | `<§X / 默认:满仓>` | `position_size()` |
| 参数优化 | `<...>` | `<§X / 默认:网格>` | `optimize()` |

## 13. 参考文档（每项引用的论文）

> 列出本策略复现所依据的论文/文献，标注用于哪一项逻辑。

| 文献 | 用于 | 关键贡献 |
|------|------|---------|
| `<Moskowitz, Ooi, Pedersen 2011, Time Series Momentum>` | `<开仓/动量>` | `<资产历史收益对未来有正向预测>` |
| `<Levine & Pedersen 2016, Which Trend is Your Friend?>` | `<开仓/指标统一性>` | `<所有线性趋势指标=历史收益加权>` |
| `<Fergis et al. 2019, Defensive Factor Timing>` | `<风控/相关性>` | `<风险分散系数监控>` |
| `<《海龟交易法则》>` | `<止盈止损>` | `<系统化退出/ATR吊灯>` |
| `<本研报自身>` | `<全部>` | `<TSI/ρ regime + 三要素实证>` |
