# cs-trend-train — 手动趋势画线交易训练工具

在 K 线图上手工画趋势线、按「等额止损」提交交易（开仓时间/开仓价/止损/止盈），
后端自动按后续行情判定平仓（**只认你提交的止损/止盈价**），并有一套独立的
交易记录与统计页面（清单 + 胜率/盈亏比/净值曲线/夏普/肥尾率）。
像 TradingView 一样画线，但线和交易记录落在本地 JSON，可反复加载复盘。

## 启动

```bash
python cs-trend-train/server.py
# → http://127.0.0.1:8710          主图（默认标的 中国平安，2025-01 ~ 2026-08）
# → http://127.0.0.1:8710/trades.html   交易记录与统计
```

零第三方 web 依赖（标准库 http.server）；数据读本地 DuckDB warehouse；
前端只引**本地化的** klinecharts / plotly（CDN 不可达也免疫）。

## 功能对照

| 需求 | 实现 |
|---|---|
| K 线：滚轮缩放 / 左键拖动 | klinecharts v10 内置（默认开启） |
| 画线：两点画 / 拖动 / 右键删除 | overlay 内置交互；`✏ 画趋势线` 进入绘制模式 |
| 画线记录 | `data/lines.json`（标的/起止日期价格/复权状态 hfq）；加载标的时重建 |
| 提交交易 | `📥 提交交易` 按钮开合面板；**开仓时间必填**（默认 = 当前最后 bar 的次日），开仓价可留空（= 该日开盘价），**止损/止盈都必填**；面板实时预览股数/开仓金额/风险 |
| 仓位反算 | `股数 = ⌊止损额度 ÷ (开仓价 − 止损价) ÷ 100⌋ × 100`（向下取整到整手，不向上放大风险） |
| 保存 / 修改 | 保存后按钮变「修改」，直接改这一笔；`＋ 新建一笔` 回到新建态 |
| 同日防重 | 唯一键 = **标的 + 开仓时间**；撞车先问「覆盖 or 载入修改」，覆盖不新增第二条 |
| 自动平仓 | 从**开仓日当根**起逐日判定：先止损后止盈（同日双触按止损计）；跳空越过触发价按**当日开盘价**成交 |
| 交易标注 | **红▲向上 = 开仓**（标在开仓日）/ **绿▼向下 = 平仓**。~~止损止盈虚线~~ 已去掉（2026-09-18：标注太多看不清，只留三角；要恢复就加回 `priceChannelLine` 那两段） |
| 标的选择 | 输入**代码 / 名称 / 拼音首字母**（如 `zxgj` → 中芯国际）模糊下拉，默认 601318.SH 中国平安 |
| 时间选择 | 起/止日期选择器，默认 2025-01-01 ~ 2026-08-31 |
| 播放 | `◀10 ◀1 ▶1 ▶10`；**▶ 智能贴边**：视图右侧尚有空档时新 bar 落进空档、已有 K 线不动，已贴右边缘才整幅滚到右边缘 |
| 未来遮蔽 | 开仓日/平仓日晚于当前最后 bar 的元素不画；**开仓日未揭示时面板也不显示该日开盘价与股数**（不提前偷看次日） |
| 设置 | `⚙ 设置`：趋势线/交易标注可见性（**默认 线开、交易关**）、全局止损额度、清除当前标的交易 |
| 交易统计 | `📊 交易统计` 新开 `trades.html`：**三个页签** —— 交易清单（标的/时间/成功/失败/肥尾 过滤 + 表头排序 + 单条删除）/ 统计指标 / 交易心得 |
| 交易心得 | `📝 交易心得` 面板：**必填 佐证标的 + 开仓日期 + 内容**（标题可留空）。佐证标的可搜（拼音），开仓日期旁列出该标的**已有交易的开仓日**可点选，默认取最近一笔 |
| 心得管理 | `trades.html → 交易心得` 页签：列表显示 标题 / 佐证标的 / 开仓日，点开看内容，可就地**修改 / 删除** |
| 交易备注 | 交易清单每行 `📝`（有备注时变黄）→ 弹窗添加 / 修改 / 删除备注；留空保存 = 删除。默认无备注 |
| 宏观行情 | `📈 宏观行情` 浮窗：**指数 close 面积图 + MA200 叠加同图**，底下一行给出「最新收盘 / MA200 / 偏离% / 在 MA200 上方还是下方」，用于判牛熊。指数可选 **沪深300 / 深证成指 / 创业板指 / 科创50**（上证仓库里没有）。**区间与主图完全一致**：起点 = 「起」日期，终点 = **当前已揭示的最后一根**，按 ◀/▶ 会自动跟着走（否则宏观窗口会显示主图还没揭示的日子 = 偷看未来） |
| 浮窗可拖 | 提交交易 / 交易心得 / 设置 / 宏观行情 四个浮窗**拖标题栏即可移动**；默认位置也让开了右侧价格标尺（`right: 84px`），不再挡住输入止损止盈时要看的价位 |
| 标的筛选（统计页） | 输入式多选：输入**代码 / 名称 / 拼音首字母**搜索 → 选中进 chip，`✕` 或退格移除，`全部` 一键清空（留空 = 全部） |

## 统计口径

只统计**已平仓**的笔（止损止盈都没触及的「持仓中」单列，不进统计）：

```
R = (平仓价 − 开仓价) / (开仓价 − 止损价)      ← 价格层量，不需要股数（老记录也能算）
胜率 = 盈利笔数 / 已平仓笔数      盈亏比 = 平均盈利R / |平均亏损R|     期望R = 平均R
肥尾率 = 收益率 ≥ +30% 占比      （与 analysis_012/014 的 is_tail = ret >= 0.3 同口径）
夏普 = 平均R / R标准差 × √(252 / 平均持仓天数)，另给不年化的「逐笔夏普」
净值曲线 = 累计 R（左轴）+ 累计盈亏（元，右轴虚线）
```

## 标的检索（代码 / 名称 / 拼音首字母）

`zxgj` → 中芯国际、`zgpa` → 中国平安、`yh` → 一堆银行。**零依赖实现**：
GB2312 的一级汉字是按拼音排序的，所以把汉字编码成 GBK、看码位落在哪个区间，
就能直接推出声母首字母（`_PY_BOUNDS`，覆盖 6763 个常用字；生僻字/字母原样保留）。

多音字单独处理：码位区间只能给最常见那个音（`行`→X），但股票名里 `银行` 要 H ——
所以对少数几个字（`_PY_AMBIG`：行/重/长/参/藏/厦…）**生成全部变体**，命中任一即匹配
（`zsyh` 与 `zsyx` 都能搜到招商银行；中国重工/重庆钢铁同理）。

标的名称取**众数**而非 `any_value`：除权日会混进 `XD中国平` 这类脏名，取错就搜不到。
名称与首字母在服务端**惰性缓存**（1578 只，一次全量查询），之后每次搜索都是内存匹配。

## API

| 端点 | 说明 |
|---|---|
| `GET /api/symbols?q=` | 代码 / 名称 / **拼音首字母**模糊搜索（全库 1578 只，索引内存缓存） |
| `GET /api/trade_symbols?q=` | 同上，但只列**有交易记录**的标的（带条数），统计页标的筛选用 |
| `GET /api/indices` | 宏观行情可选指数（仓库 index_bar1d 里那 4 个） |
| `GET /api/index_klines?symbol=&start=` | 指数日线（读 index_bar1d；注意它的 date 是 TIMESTAMP，与股票的 YYYYMMDD 串不同） |
| `GET /api/klines?symbol=&start=&end=` | K 线 + ATR14 + 原始价（后复权为主口径） |
| `GET/POST/DELETE /api/lines?symbol=` | 画线记录 CRUD |
| `GET /api/trades?symbol=` | 该标的交易记录（**读时补派生量**：R/收益率/盈亏/持仓天数/状态） |
| `GET /api/trades?all=1` | 全部标的 + 代码→名称映射（统计页用） |
| `POST /api/trades` | 新建/修改；`?dry_run=1` 只算不落库（面板预览用）；同日撞车返回 **409** + `existing`，带 `overwrite:true` 才覆盖 |
| `DELETE /api/trades?symbol=&id=` | 删单条 |
| `DELETE /api/trades?symbol=&all=1` | 清空该标的（设置页用，独立分支，不复用上面的 id 逻辑） |
| `GET/POST /api/settings` | 全局设置 `{risk_amount, show_lines, show_trades}` |
| `GET /api/notes` | 全部交易心得（扁平列表，带 symbol→名称） |
| `POST /api/notes` | 新建/修改心得（带 id = 修改）；必填 标的 / 开仓日期 / 内容 |
| `DELETE /api/notes?id=` | 删一条心得 |
| `POST /api/trade_note` | 只改某笔交易的备注 `{symbol, id, note}`（空串 = 删除备注），不动其它字段 |

## 数据

- 行情：warehouse `stock_bar1d`（后复权；`raw` 字段 = 后复权 ÷ adjust_factor）
- ATR14：Wilder ewm(α=1/14)（与 hs300 工程 `shared/atr14_precompute.py` 同式）——当前**出场判定不用它**，仅随 K 线下发备用
- 记录：`data/lines.json` / `data/trades.json` / `data/settings.json`（按标的分组，肉眼可查可手改）
  + `data/notes.json`（交易心得，**扁平列表**不按标的分组 —— 统计页要跨标的看）
- 交易备注：存在交易记录自己的 `note` 字段里。⚠ **改交易记录时必须把 note 带过去**：
  `build_trade()` 只产出事实字段、不产出 note，upsert 时若不从旧记录搬一次，
  用户写的备注会被「修改交易」静默清掉（后端已处理）
- 交易字段：`entry_date / entry_source / buy_price / stop_price / tp_price / risk_amount / shares`
  （**写时的事实**）+ `exit_date / exit_price / exit_reason / holding_days`（判定结果），
  其余（R / 收益率 / 盈亏 / 状态）都是**读时算**的，改口径不必迁移历史

## 已知限制

1. **后复权价会被新除权事件回溯改写**：仓库刷新后整段历史 hfq 价平移，而判定用的是
   「当下的 klines」，与当初存进 JSON 的 `buy_price/stop_price` 可能不再同一坐标系 →
   止损线会错位。记录里存了 `adj:"hfq"` 备查
2. 同日既触止损又触止盈 → 按止损计（日内路径不可知）
3. 不计交易成本（毛收益）
4. 老记录（上一版 schema，无 `entry_date`/`shares`）：开仓日退回信号日、R 照常算，
   但股数/盈亏显示「—」（不反填止损额度，避免凭空造出从未发生过的仓位）
